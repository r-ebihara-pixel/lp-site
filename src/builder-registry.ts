/**
 * Builder.io コンポーネント登録ファイル
 * 
 * このファイルでregisterComponentを使って、
 * Builder.ioのVisual Editorで使用できるカスタムコンポーネントを登録します。
 * 
 * 例:
 * import { Builder } from "@builder.io/sdk-react-nextjs";
 * import { HeroSection } from "@/components/sections/hero-section";
 * 
 * Builder.registerComponent(HeroSection, {
 *   name: "HeroSection",
 *   inputs: [
 *     { name: "title", type: "string", required: true },
 *     { name: "subtitle", type: "string" },
 *   ],
 * });
 * 
 * TODO: 次のフェーズで以下のコンポーネントを登録予定
 * - Section / Container / Stack / Grid / Text / Button / Image
 * - 既存LPのHero/FAQ/CTAコンポーネント
 */

// 現在は空 - 将来的にここにregisterComponentを追加していく
export {};
